import NetwordDirectory from "@/components/shared/NetwordDirectory";
import React from "react";

const NetwordDirectoryPage = () => {
  return <NetwordDirectory />;
};

export default NetwordDirectoryPage;
